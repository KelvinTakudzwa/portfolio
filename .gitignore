# dependencies (unanchored: also covers job-watcher/node_modules etc.)
node_modules/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# sensitive source documents: never commit these (license, transcript,
# certificates — identity/verification documents, kept local only).
# The safe media (capping photo, cheque photo, certificate) already lives
# in public/images/ and is tracked normally.
/src/files/

# harness-internal, not part of the project
/.claude/

.vercel
.env*
